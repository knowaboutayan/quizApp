import React from 'react';
import Erro

const NotFoundPage = () => {
  return (
                <div className="flex center-align">
                <ErrorBox icon={} errorText="page not found " navigateTo="/" navigateText="back to home" />
            </div>
  );
};

export default NotFoundPage;
